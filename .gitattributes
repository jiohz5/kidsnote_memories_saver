# 배치 파일은 반드시 CRLF로 체크아웃되어야 한다.
# cmd.exe는 파일을 CRLF 기준 바이트 오프셋으로 읽기 때문에, LF로만 저장되면
# goto 라벨과 괄호 블록이 어긋나 'XXX은(는) 내부 명령이 아닙니다' 류의
# 엉뚱한 오류를 내며 빌드가 조용히 실패한다. 실제로 V1.08 빌드에서 재발했다.
*.bat text eol=crlf
*.cmd text eol=crlf
